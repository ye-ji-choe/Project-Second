using UnityEngine;

namespace ImmersiveTraining.StateHandling
{
    public class HideInsteadOfDeactivate : MonoBehaviour
    {
        /*  Component only used to tag objects that should have all their renderers and colliders disabled during state deactivation instead of disabling the game object
    //  Also state activation will only enable these components instead of enabling the game object
    */
    }
}
